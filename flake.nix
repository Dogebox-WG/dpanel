{
  inputs = {
    nixpkgs.url     = "github:NixOS/nixpkgs/nixos-25.11";
    flake-utils.url = "github:numtide/flake-utils";

    dogeboxd-src = {
      # Pinned to the DTO schema branch while it is in review; move to main
      # once dogeboxd PRs land.
      url = "github:Dogebox-WG/dogeboxd/feature/dto-protos";
      flake = false;
    };

    protovalidate-src = {
      url = "github:bufbuild/protovalidate";
      flake = false;
    };
  };
  
  outputs = inputs@{ self, nixpkgs, flake-utils, dogeboxd-src, protovalidate-src, ... }:
    flake-utils.lib.eachDefaultSystem (system:
      let
        pkgs = import nixpkgs { inherit system; };
      in {
        devShells.default = pkgs.mkShell {
          buildInputs = [ pkgs.nodejs_24 pkgs.screen pkgs.buf
            pkgs.chromium
          ];

          shellHook = ''
            export PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
            export PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH="${pkgs.chromium}/bin/chromium"
            export DBX_BUF_EXECUTABLE="${pkgs.buf}/bin/buf"

            if [ ! -d node_modules/@deform-wg/deform ]; then
              echo "Installing root npm dependencies..."
              npm install
            fi
          '';
        };

        packages.default = pkgs.buildNpmPackage {
          name = "dpanel";
          src = ./.;

          npmDepsHash = "sha256-Ud6fUHy+Edfkcb3syp3iSVBLWGodxHDl6WbMxxj0iD0=";

          nativeBuildInputs = [ pkgs.protobuf ];

          preBuild = ''
            export PATH="$PWD/node_modules/.bin:$PATH"
            mkdir -p src/gen
            find ${dogeboxd-src}/protocol ${protovalidate-src}/proto/protovalidate \
              -name '*.proto' -print0 \
              | xargs -0 protoc \
                --es_out=src/gen \
                --es_opt=target=ts \
                -I ${dogeboxd-src}/protocol \
                -I ${protovalidate-src}/proto/protovalidate
          '';

          buildPhase = ''
            runHook preBuild
            # Protos are already generated by preBuild from the pinned
            # dogeboxd-src input; --ignore-scripts skips the npm prebuild
            # hook (npx buf generate), which expects ../dogeboxd on disk.
            npm run build --ignore-scripts
            runHook postBuild
          '';

          installPhase = ''
            cp -r dist $out
          '';

          meta = with pkgs.lib; {
            description = "Dogebox control panel web interface";
            homepage = "https://github.com/Dogebox-WG/dpanel";
            license = licenses.mit;
          };
        };

        packages.build-with-dev-overrides = pkgs.writeShellApplication {
          name = "build-with-dev-overrides";
          runtimeInputs = [ pkgs.git ];
          text = ''
            nix build .#packages.${system}.default \
              -L \
              --print-out-paths \
              --override-input dogeboxd-src "path:$(realpath ../dogeboxd)?rev=$(git -C ../dogeboxd log -1 --pretty=format:%H)" \
              --no-write-lock-file
          '';
        };

        packages.test = pkgs.writeShellApplication {
          name = "dpanel-test";
          runtimeInputs = [ pkgs.nodejs_24 pkgs.chromium ];
          text = ''
            set -euo pipefail

            if [ ! -f package.json ] || [ ! -d dev ]; then
              echo "Run dpanel-test from the dpanel repository root." >&2
              exit 1
            fi

            if [ ! -d node_modules/@deform-wg/deform ]; then
              echo "Installing root npm dependencies..."
              npm install
            fi

            cd dev
            npm install

            export PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
            export PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH="${pkgs.chromium}/bin/chromium"
            exec npm test
          '';
        };

        apps.test = {
          type = "app";
          program = "${self.packages.${system}.test}/bin/dpanel-test";
        };

        dbxSessionName = "dpanel";
        dbxStartCommand = "npm run dev";
      }
    );
}
