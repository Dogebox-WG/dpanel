import { expect } from "../../../../../dev/node_modules/@open-wc/testing";

describe("Deliberate failure (remove this test)", () => {
  it("should fail on purpose to test Discord notifications", () => {
    expect(true).to.equal(false);
  });
});
