import { html, css } from "/lib/lit-all.js";

export function renderFooter() {
  const footerStyles = css``;

  return html`
    <footer>
      <style>
        ${footerStyles}
      </style>
    </footer>
  `;
}
